import React, { useEffect } from 'react';
import * as THREE from 'three';

function Line() {
    const init = () => {};
    useEffect(() => {
        init();
    }, []);

    return <div id="content"></div>;
}

export default Line;
